import PageBuilder from '../../components/PageBuilder';
import { AltaiConfig } from './altai.config';

const AltaiPage = () => {
  return <PageBuilder sectionsConfig={AltaiConfig} />;
};

export default AltaiPage;
